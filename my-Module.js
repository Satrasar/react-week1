 const hello = (name) => {
    //console.log("hello " + name);
    console.log(`hello ${name}`)

};

 const topla = (a, b) => a + b; 
 const cikar = (a, b) => a - b;


 export const users = [{
    name : 'ozlem',
    surname : 'kumtas'
},{
    name : 'elif',
    surname :'kumtas',
},
]; 


 export{hello, topla, cikar}
